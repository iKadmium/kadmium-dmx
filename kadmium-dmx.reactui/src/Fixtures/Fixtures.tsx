import React, { Component } from 'react';
import { Page } from '../Page/Page';

export const Fixtures: React.FunctionComponent<{}> = () =>
{
    return (
        <Page>
            Fixtures
        </Page>
    );
}
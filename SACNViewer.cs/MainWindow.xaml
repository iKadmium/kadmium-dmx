<Window x:Class="SACNViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SACNViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        
        <Style TargetType="Rectangle" x:Key="RectStyle">
            <Setter Property="Stroke" Value="Black"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Margin" Value="4"/>
        </Style>

        <ControlTemplate x:Key="DMXTemplate">
            <Rectangle Style="{StaticResource RectStyle}" Fill="{Binding Path=Brush}" />
        </ControlTemplate>
        <ControlTemplate x:Key="RowTemplate">
            <StackPanel Orientation="Horizontal"/>
        </ControlTemplate>
        
    </Window.Resources>
    
    <StackPanel Orientation="Vertical" x:Name="stkRows">
        


    </StackPanel>
</Window>
